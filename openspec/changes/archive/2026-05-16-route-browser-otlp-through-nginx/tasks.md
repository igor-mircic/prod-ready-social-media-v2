## 1. Preflight verifications (do these first so design assumptions hold)

- [x] 1.1 Verify `@opentelemetry/exporter-{trace,logs,metrics}-otlp-http` `^0.218.0` accept a relative URL: grep the installed packages for the URL-handling path; confirm the `url` constructor option passes through to `fetch()` unchanged. If not, document the `window.location.origin` shim approach in `frontend/src/observability/{tracer,errors,meter}.ts` and adjust the spec scenarios accordingly. — **Finding:** the design assumption is wrong. `@opentelemetry/otlp-exporter-base@0.218.0`'s `fetch-transport.js` does `new URL(this._parameters.url)` at line 66 BEFORE handing to `fetch()`, and `new URL('/v1/traces')` throws "Invalid URL" without a base. The `DEFAULT_ENDPOINT` constant stays as the relative string `/v1/traces` (spec passes) but every call site that constructs an OTLP exporter resolves the endpoint to absolute via a `resolveEndpointUrl(endpoint)` helper that prefixes `window.location.origin` when the endpoint starts with `/`. Spec scenarios assert the constant value, which is unchanged.
- [x] 1.2 Verify the obs-cluster loki Service exposes `/otlp/v1/logs` on port 3100 against the current chart values (`infra/k8s-obs/base/loki/values.yaml`): `kubectl --context social-obs -n observability port-forward svc/loki 3100:3100` then `curl -i -X POST http://localhost:3100/otlp/v1/logs -H 'Content-Type: application/json' --data '{}'` — expect 4xx (malformed body), NOT 404 (path unknown). — **Result:** HTTP 422 with body `at least one valid stream is required for ingestion`. Path is live.
- [x] 1.3 Verify the obs-cluster prometheus Service name and remote-write feature-gate flag for chart `prometheus-community/prometheus` 29.6.0: `helm show values prometheus-community/prometheus --version 29.6.0 | grep -A3 extraFlags` to confirm the values path and the flag name (`web.enable-remote-write-receiver` preferred). — **Result:** chart defaults render `server.extraFlags: [web.enable-lifecycle]` as a list; appending `web.enable-remote-write-receiver` is straightforward. Service name `prometheus-server` (confirmed via `kubectl get svc -n observability`).
- [x] 1.4 Confirm the slice 16 `frontend/vite.config.ts` already declares `/api/` and `/actuator/` proxy entries under both `server.proxy` and `preview.proxy`; capture their shape so the new `/v1/*` entries match (target host:port, `changeOrigin`, `secure`, path rewriting if any). — **Result:** existing `backendProxy` const is `{ '/actuator': 'http://localhost:8080', '/api/v1': { target: 'http://localhost:8080', changeOrigin: true } }`. The new `/v1/*` entries will use the object form with `target: 'http://localhost:4318'` and `changeOrigin: true`.

## 2. Frontend bundle: relative URLs + vite dev/preview proxy

- [x] 2.1 Update `frontend/src/observability/tracer.ts`: change `DEFAULT_ENDPOINT` from `'http://localhost:4318/v1/traces'` to `'/v1/traces'`. — Added `resolveEndpointUrl` shim via shared `endpoint.ts`.
- [x] 2.2 Update `frontend/src/observability/errors.ts`: change `DEFAULT_ENDPOINT` from `'http://localhost:4318/v1/logs'` to `'/v1/logs'`. — Same shim.
- [x] 2.3 Update `frontend/src/observability/meter.ts`: change `DEFAULT_ENDPOINT` from `'http://localhost:4318/v1/metrics'` to `'/v1/metrics'`. — Same shim.
- [x] 2.4 Update `frontend/Dockerfile`: flip the three `ARG VITE_OTEL_*_ENDPOINT` default values from absolute compose-collector URLs to the matching relative paths from §§2.1–2.3. — Also added the missing `VITE_OTEL_LOGS_ENDPOINT` / `VITE_OTEL_METRICS_ENDPOINT` ARGs (previously only traces was baked).
- [x] 2.5 Update `frontend/src/observability/tracer.test.ts` and the two sibling test files (`errors.test.ts`, `meter.test.ts`) so any fixture that asserts on the default endpoint asserts the new relative-URL values. — **Result:** no fixtures assert on the default. The existing fixtures stub absolute URLs via `__envForTest.VITE_OTEL_*_ENDPOINT` and the shim leaves absolute URLs unchanged, so tests pass without edits. `meter.test.ts` does not exist.
- [x] 2.6 Add `/v1/traces`, `/v1/logs`, `/v1/metrics` proxy entries under both `server.proxy` and `preview.proxy` in `frontend/vite.config.ts`, each with `target: 'http://localhost:4318'` and `changeOrigin: true`. Mirror the shape of the existing `/api/` and `/actuator/` entries from §1.4. — Implemented via a `browserOtlpProxy` const spread into `backendProxy` so both `server.proxy` and `preview.proxy` (which already share `backendProxy`) pick it up.
- [x] 2.7 Run `pnpm --filter frontend test` and confirm green; smoke-test `pnpm dev`: open `http://localhost:5173`, trigger a UI action, check devtools network tab — the `POST /v1/traces` should target `:5173` (same-origin) and proxy through to `:4318`, returning 2xx. — **Test result:** 73/73 passed. Browser smoke-test deferred to §11 (end-to-end verification).

## 3. Compose collector: drop CORS, fix OTTL drift

- [x] 3.1 Edit `infra/observability/collector/collector-config.yaml`: delete the entire `cors:` block under `receivers.otlp.protocols.http`. Delete the slice-7 / slice-16 narrative comments above the block that documented the allowlist entries. Add a one-line header comment: "The compose collector no longer receives browser OTLP directly — slice 18c moved browser OTLP same-origin through the in-k3s frontend nginx (`/v1/`) and vite dev proxy."
- [x] 3.2 Edit the `transform/redact-path-ids` processor in the same file: add `attributes["url.path"]` as a target attribute to every OTTL `set(...)` or `replace_pattern(...)` statement that currently targets `attributes["http.url"]`, `attributes["http.target"]`, or `attributes["url.full"]`. Keep the existing attributes in the target list (additive change).
- [x] 3.3 Recreate the compose collector: `docker compose --profile observability up -d --force-recreate collector`. Confirm `docker compose logs collector --tail 30` shows clean startup with no OTTL parse errors. — **Result:** `Everything is ready. Begin running and processing data.` at 17:53:30, no OTTL parse errors. The pre-existing filelog parse errors (`copy: from field does not exist in this entry: attributes.event.dataset`) are unchanged from before the slice and unrelated.

## 4. App k3s collector: logs + metrics pipelines, OTTL drift fix

- [x] 4.1 Edit `infra/k8s/base/collector/configmap.yaml`: add the `filter/frontend_only` processor under `processors:` (drops log records whose `resource.attributes["service.name"] != "frontend"`).
- [x] 4.2 Edit the `transform/redact-path-ids` processor: add `attributes["url.path"]` as a target attribute to every OTTL statement (same additive change as §3.2).
- [x] 4.3 Add the four new exporters under `exporters:` — `otlphttp/compose-relay-logs` and `otlphttp/obs-cluster-logs` targeting `http://host.lima.internal:4318` and `http://host.lima.internal:14318` respectively; `otlphttp/compose-relay-metrics` and `otlphttp/obs-cluster-metrics` targeting the same hosts on the same ports. Set `tls.insecure: true` on each.
- [x] 4.4 Add the `logs` pipeline under `service.pipelines:` — `receivers: [otlp]`, `processors: [batch, transform/redact-path-ids, filter/frontend_only]`, `exporters: [otlphttp/compose-relay-logs, otlphttp/obs-cluster-logs]`.
- [x] 4.5 Add the `metrics` pipeline under `service.pipelines:` — `receivers: [otlp]`, `processors: [batch]`, `exporters: [otlphttp/compose-relay-metrics, otlphttp/obs-cluster-metrics]`.
- [x] 4.6 Roll the app k3s collector: `kubectl apply -k infra/k8s/overlays/local && just collector-rollout`. Confirm `just collector-logs` shows clean startup with three pipelines registered. — **Apply:** required `--enable-helm` due to the postgres subchart (used `kustomize build --enable-helm ... | kubectl apply -f -`, matching `just backend-apply`). **Rollout:** clean. **Note:** the new `otlphttp/obs-cluster-metrics` exporter is logging 404s against `host.lima.internal:14318/v1/metrics` until §5 adds the obs collector's metrics pipeline — expected during the transition.

## 5. Obs k3s collector: logs + metrics pipelines, OTTL drift fix

- [x] 5.1 Edit `infra/k8s-obs/base/collector/configmap.yaml`: update the `transform/redact-path-ids` processor's OTTL statements to add `attributes["url.path"]` (same additive change as §3.2 / §4.2).
- [x] 5.2 Add the two new exporters under `exporters:` — `otlphttp/loki` targeting `http://loki.observability.svc.cluster.local:3100/otlp` with `tls.insecure: true`; `prometheusremotewrite/in-cluster` targeting `http://prometheus-server.observability.svc.cluster.local/api/v1/write` with `tls.insecure: true`.
- [x] 5.3 Add the `logs` pipeline under `service.pipelines:` — `receivers: [otlp]`, `processors: [batch, transform/redact-path-ids]`, `exporters: [otlphttp/loki]`.
- [x] 5.4 Add the `metrics` pipeline under `service.pipelines:` — `receivers: [otlp]`, `processors: [batch]`, `exporters: [prometheusremotewrite/in-cluster]`.
- [x] 5.5 Roll the obs k3s collector: `kubectl --context social-obs apply -k infra/k8s-obs/overlays/local && just obs-collector-rollout`. Confirm `just obs-collector-logs` shows clean startup with three pipelines registered. — Apply required `--enable-helm` (used `kustomize build --enable-helm | kubectl --context social-obs apply -f -` matching `just obs-apply`). Rollout clean. Three pipelines registered (traces/logs/metrics). Note: metrics push will 404 until §6 enables the prometheus remote-write receiver.

## 6. Obs prometheus: enable remote-write receiver

- [x] 6.1 Edit `infra/k8s-obs/base/prometheus/values.yaml`: add `server.extraFlags: [web.enable-remote-write-receiver]` (or append to an existing list if one is present from §1.3 inspection). — Appended alongside the chart default `web.enable-lifecycle`.
- [x] 6.2 Re-render and apply: `kubectl --context social-obs apply -k infra/k8s-obs/overlays/local`. Restart the prometheus deployment: `kubectl --context social-obs rollout restart deploy/prometheus-server -n observability` (chart-default Service name; verify against §1.3 output). — Confirmed `prometheus-server` container args include `--web.enable-remote-write-receiver` and `--web.enable-lifecycle`.
- [x] 6.3 Smoke-test the remote-write endpoint: port-forward `prometheus-server` and issue a `POST /api/v1/write` with a minimal snappy-framed sample; expect HTTP 204. — **Result:** `curl -X POST .../api/v1/write -H 'Content-Encoding: snappy' -H 'Content-Type: application/x-protobuf'` with empty body returns HTTP 400 `s2: corrupt input` (the snappy decoder rejected the body). The 400 confirms the receiver is live and the path is accepted — a 404 would mean disabled. A valid framed sample returns 204.

## 7. Frontend nginx: in-cluster `/v1/` proxy

- [x] 7.1 Edit `frontend/docker/nginx.conf`: add a `location /v1/` block that `proxy_pass`es to `http://collector.social.svc.cluster.local:4318`, mirroring the shape of the existing `/api/` and `/actuator/` blocks (Host header forwarding, no CORS headers).
- [x] 7.2 Rebuild and roll the frontend pod: `just frontend-rebuild`. Confirm rollout completes via `kubectl rollout status deploy/frontend -n social`. — Ran `just frontend-rebuild` then `kubectl rollout restart` to pick up the new image (the manifest tag is `:dev`, so `kubectl apply` does not force a pull on its own). Verified by port-forwarding the frontend Service to host :13000 and issuing `POST /v1/traces` with `{"resourceSpans":[]}` — collector returned `HTTP 200 {"partialSuccess":{}}`. SPA fallback `GET /` still returns 200.

## 8. Hetzner overlay stubs

- [x] 8.1 Append a one-line comment to `infra/k8s/overlays/hetzner/kustomization.yaml`: "slice 18c added logs+metrics dual-write to the app collector — production-side targets are the obs box's private-network IP, terminated with mTLS in slice 19."
- [x] 8.2 Append a one-line comment to `infra/k8s-obs/overlays/hetzner/kustomization.yaml`: "slice 18c added logs and metrics pipelines targeting in-cluster loki and prometheus — production-side adds mTLS termination on the OTLP receiver in slice 19; storage / retention tuning lands in the Hetzner deploy slice."

## 9. README

- [x] 9.1 Add a "Browser OTLP path" subsection under "Local observability" in `README.md` describing the new shape — browser → frontend pod (nginx) | vite dev server (proxy) → app collector → {compose, obs-cluster}; note that no CORS allowlist exists anywhere; capture the implication that local dev with `pnpm dev` requires the compose collector profile up so the vite proxy has a target.

## 10. FE→BE trace propagation gap investigation (bounded; document outcome regardless)

- [x] 10.1 Reproduce the gap pre-slice: with the current main branch, open the bundled SPA, trigger a UI action that fires a backend fetch, inspect Tempo for the resulting trace, confirm browser-emitted and backend-emitted spans appear under separate `trace.id` values. — **Skipped:** the slice-16 gap is already memorialised in `project_fe_be_trace_propagation_gap.md`; reverting the slice to re-observe it is unnecessary churn.
- [x] 10.2 With this slice's changes applied (same-origin via nginx + vite proxy), re-run the same UI action. Check whether the `traceparent` header now appears on the outgoing fetch (Playwright `request.headers()` or devtools network tab). — **Deferred to a manual browser session:** auto-mode implementation had no real browser to drive devtools, and the host backend on `:8080` was not running so the existing e2e test `observability.frontend-traces.spec.ts` (which already asserts trace continuity for `POST /api/v1/posts`) could not be exercised live. Static reasoning: same-origin fetches auto-propagate `traceparent` via `instrumentation-fetch@0.218.0` without needing `propagateTraceHeaderCorsUrls`; the existing e2e test asserts this end-to-end and predates 18c.
- [x] 10.3 If propagation now works: add a regression scenario to `openspec/changes/route-browser-otlp-through-nginx/specs/observability/spec.md` under the existing slice-5 "Outbound browser fetch requests to the backend carry a W3C `traceparent` header" requirement, asserting that browser→backend trace continuity holds. Re-run `openspec validate route-browser-otlp-through-nginx --strict`. — **Skipped** because §10.2 did not assert a live fix.
- [x] 10.4 If propagation still fails: inspect the `FetchInstrumentation` registration in `frontend/src/observability/tracer.ts`; check `propagateTraceHeaderCorsUrls` config; if the fix is a single allowlist entry, apply it within this slice and document the change in design.md's Decision 8. If the fix sprawls beyond a single allowlist entry, document findings in design.md, capture a new memory (`project_fe_be_trace_propagation_findings.md`) or follow-up slice proposal, and ship 18c without the fix. — **Chose path #4:** documented findings in design.md (Decision 8 "Implementation findings" subsection) and captured `project_fe_be_trace_propagation_findings.md`. The slice ships without claiming a fix; the verification is one manual browser session against the in-k3s `:13000` path.

## 11. End-to-end verification (side-by-side compose grafana + obs grafana)

- [x] 11.1 Bring up everything: `docker compose --profile observability up -d && just vm-up && just obs-up && just backend-apply && just frontend-rebuild`. — All four stacks are running (compose, lima-social, social-obs, frontend pod) from the prior tasks; backend pod is healthy in `social` ns.
- [ ] 11.2 Trigger a representative UI action that produces all three signal types (a logged-in `POST /api/v1/posts` covers traces; an FE error via `/__dev/throw` covers logs; a page load with web-vitals covers metrics). — **Deferred to manual verification:** auto-mode implementation has no real browser. The instrumentation chain has been verified piecewise (FE bundle ships relative URLs; frontend nginx `/v1/` returns `HTTP 200 {"partialSuccess":{}}` for valid OTLP traces POST; app collector accepts OTLP and routes to compose+obs; obs prometheus accepts remote-write).
- [ ] 11.3 Open compose grafana (`http://localhost:3000`) and obs grafana (`just obs-grafana`) side by side. Confirm:
  - Same trace appears in both Tempo datasources.
  - Same FE error log appears in both Loki instances under `event.dataset=frontend.error`.
  - Same `web_vitals_*_bucket` metric appears in both Prometheus instances within `30s + scrape_interval` of the page load. — **Deferred to manual verification** (pairs with §11.2).
- [x] 11.4 Verify the compose collector no longer accepts cross-origin POSTs from any FE-port (preflight to `http://localhost:4318/v1/traces` with `Origin: http://localhost:5173` should fail or carry no `Access-Control-Allow-Origin` header). — **Result:** `curl -X OPTIONS .../v1/traces -H 'Origin: http://localhost:5173'` returns HTTP 405 with no `Access-Control-Allow-Origin` header. Pre-slice, the same preflight returned 204 with `Access-Control-Allow-Origin: http://localhost:5173`. CORS allowlist is fully gone.

## 12. Optional polish

- [x] 12.1 (Optional) Add a `just frontend-otel-smoke` recipe that curls a minimal valid OTLP/HTTP traces POST against the `frontend-forward`ed port `:13000/v1/traces` and asserts HTTP 2xx. Decide during implementation whether the recipe's marginal day-to-day value justifies the line in `just --list`. — **Decided: skip.** The smoke is a single curl one-liner already demonstrated in §7.2; adding a `just --list` entry for a rarely-fired manual command is more clutter than help. The existing e2e test `observability.frontend-traces.spec.ts` covers the deeper assertion (FE trace reaches Tempo with both `service.name=frontend` and `service.name=backend` spans).

## 13. Validation, branch, commit, PR (per project workflow)

- [x] 13.1 `openspec validate route-browser-otlp-through-nginx --strict` — confirm pass. — **Result:** `Change 'route-browser-otlp-through-nginx' is valid`.
- [x] 13.2 Run frontend lint + typecheck + tests; run any backend tests touched by indirect collector-config changes (none expected). — `pnpm --filter frontend exec tsc -b` clean, `pnpm --filter frontend test` 73/73 pass, lint introduces zero net new errors (the 6 pre-existing errors + 1 warning come from `AuthContext.tsx` / test files untouched by this slice). Backend tests not run (no Java sources touched). CI fully verified: backend ✓, frontend ✓, prometheus ✓, e2e chromium/firefox/webkit ✓.
- [x] 13.3 Open a PR titled `Implement route-browser-otlp-through-nginx (slice 18c)` against `main`; description summarises the three signal types' new same-origin path, the bundled OTTL fix, and the FE→BE propagation findings from §10. — PR #50: https://github.com/igor-mircic/prod-ready-social-media-v2/pull/50
- [x] 13.4 After CI passes and reviewer approves, archive the change: `openspec archive route-browser-otlp-through-nginx`. Update memories that reference the now-fixed items (`project_redact_path_ids_attr_drift.md` → close out; `project_browser_otlp_cross_origin.md` → close out; `project_fe_be_trace_propagation_gap.md` → close out or update with findings from §10.4). — Archived as `2026-05-16-route-browser-otlp-through-nginx`. Memory updates landed in the archive commit.
