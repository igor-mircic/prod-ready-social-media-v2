{
  "annotations": {
    "list": [
      {
        "builtIn": 1,
        "datasource": {
          "type": "grafana",
          "uid": "-- Grafana --"
        },
        "enable": true,
        "hide": true,
        "iconColor": "rgba(0, 211, 255, 1)",
        "name": "Annotations & Alerts",
        "type": "dashboard"
      }
    ]
  },
  "editable": false,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 0,
  "id": null,
  "links": [],
  "liveNow": false,
  "panels": [
    {
      "id": 1,
      "title": "LCP p75",
      "type": "timeseries",
      "gridPos": { "h": 8, "w": 5, "x": 0, "y": 0 },
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "fieldConfig": { "defaults": { "unit": "ms" }, "overrides": [] },
      "options": {},
      "targets": [
        {
          "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
          "expr": "histogram_quantile(0.75, sum by (le) (rate(web_vitals_lcp_bucket{service_name=\"frontend\"}[5m])))",
          "legendFormat": "p75",
          "refId": "A"
        }
      ]
    },
    {
      "id": 2,
      "title": "CLS p75",
      "type": "timeseries",
      "gridPos": { "h": 8, "w": 5, "x": 5, "y": 0 },
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "fieldConfig": { "defaults": { "unit": "short" }, "overrides": [] },
      "options": {},
      "targets": [
        {
          "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
          "expr": "histogram_quantile(0.75, sum by (le) (rate(web_vitals_cls_bucket{service_name=\"frontend\"}[5m])))",
          "legendFormat": "p75",
          "refId": "A"
        }
      ]
    },
    {
      "id": 3,
      "title": "INP p75",
      "type": "timeseries",
      "gridPos": { "h": 8, "w": 5, "x": 10, "y": 0 },
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "fieldConfig": { "defaults": { "unit": "ms" }, "overrides": [] },
      "options": {},
      "targets": [
        {
          "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
          "expr": "histogram_quantile(0.75, sum by (le) (rate(web_vitals_inp_bucket{service_name=\"frontend\"}[5m])))",
          "legendFormat": "p75",
          "refId": "A"
        }
      ]
    },
    {
      "id": 4,
      "title": "FCP p75",
      "type": "timeseries",
      "gridPos": { "h": 8, "w": 5, "x": 15, "y": 0 },
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "fieldConfig": { "defaults": { "unit": "ms" }, "overrides": [] },
      "options": {},
      "targets": [
        {
          "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
          "expr": "histogram_quantile(0.75, sum by (le) (rate(web_vitals_fcp_bucket{service_name=\"frontend\"}[5m])))",
          "legendFormat": "p75",
          "refId": "A"
        }
      ]
    },
    {
      "id": 5,
      "title": "TTFB p75",
      "type": "timeseries",
      "gridPos": { "h": 8, "w": 4, "x": 20, "y": 0 },
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "fieldConfig": { "defaults": { "unit": "ms" }, "overrides": [] },
      "options": {},
      "targets": [
        {
          "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
          "expr": "histogram_quantile(0.75, sum by (le) (rate(web_vitals_ttfb_bucket{service_name=\"frontend\"}[5m])))",
          "legendFormat": "p75",
          "refId": "A"
        }
      ]
    },
    {
      "id": 6,
      "title": "Route change duration p50 / p95 / p99 by route",
      "type": "timeseries",
      "gridPos": { "h": 8, "w": 24, "x": 0, "y": 8 },
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "fieldConfig": {
        "defaults": {
          "custom": { "drawStyle": "line", "fillOpacity": 10, "stacking": { "mode": "none" } },
          "unit": "ms"
        },
        "overrides": []
      },
      "options": {},
      "targets": [
        {
          "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
          "expr": "histogram_quantile(0.50, sum by (le, route) (rate(route_change_duration_ms_bucket{service_name=\"frontend\"}[5m])))",
          "legendFormat": "p50 {{route}}",
          "refId": "A"
        },
        {
          "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
          "expr": "histogram_quantile(0.95, sum by (le, route) (rate(route_change_duration_ms_bucket{service_name=\"frontend\"}[5m])))",
          "legendFormat": "p95 {{route}}",
          "refId": "B"
        },
        {
          "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
          "expr": "histogram_quantile(0.99, sum by (le, route) (rate(route_change_duration_ms_bucket{service_name=\"frontend\"}[5m])))",
          "legendFormat": "p99 {{route}}",
          "refId": "C"
        }
      ]
    },
    {
      "id": 7,
      "title": "Long task rate",
      "type": "timeseries",
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 16 },
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "fieldConfig": { "defaults": { "unit": "ops" }, "overrides": [] },
      "options": {},
      "targets": [
        {
          "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
          "expr": "rate(long_task_duration_ms_count{service_name=\"frontend\"}[5m])",
          "legendFormat": "long tasks/s",
          "refId": "A"
        }
      ]
    },
    {
      "id": 8,
      "title": "Long task mean duration",
      "type": "timeseries",
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 16 },
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "fieldConfig": { "defaults": { "unit": "ms" }, "overrides": [] },
      "options": {},
      "targets": [
        {
          "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
          "expr": "rate(long_task_duration_ms_sum{service_name=\"frontend\"}[5m]) / rate(long_task_duration_ms_count{service_name=\"frontend\"}[5m])",
          "legendFormat": "mean ms",
          "refId": "A"
        }
      ]
    },
    {
      "id": 9,
      "title": "Browser request volume (LCP-derived sessions/min)",
      "type": "timeseries",
      "gridPos": { "h": 8, "w": 24, "x": 0, "y": 24 },
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "fieldConfig": { "defaults": { "unit": "short" }, "overrides": [] },
      "options": {},
      "targets": [
        {
          "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
          "expr": "rate(web_vitals_lcp_count{service_name=\"frontend\"}[1m]) * 60",
          "legendFormat": "sessions/min (proxy)",
          "refId": "A"
        }
      ]
    }
  ],
  "refresh": "10s",
  "schemaVersion": 39,
  "tags": ["frontend", "observability", "rum"],
  "templating": {
    "list": [
      {
        "name": "DS_PROMETHEUS",
        "label": "Prometheus",
        "type": "datasource",
        "query": "prometheus",
        "regex": "",
        "current": {
          "selected": false,
          "text": "Prometheus",
          "value": "prometheus"
        },
        "hide": 2,
        "refresh": 1,
        "skipUrlSync": false,
        "multi": false,
        "includeAll": false,
        "options": []
      }
    ]
  },
  "time": { "from": "now-15m", "to": "now" },
  "timepicker": {},
  "timezone": "browser",
  "title": "Frontend overview",
  "uid": "frontend-overview",
  "version": 1,
  "weekStart": ""
}
